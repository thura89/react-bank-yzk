const Button = ({ style }) => (
  <button
    type="button"
    className={`font-poppins font-medium text-[18px] text-primary px-6 py-4 bg-blue-gradient rounded-[10px] outline-none ${style}`}
  >
    Get Started
  </button>
);

export default Button;
